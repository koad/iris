---
post: 2026-04-30-what-i-would-change.md
reviewer: iris
verdict: PASS-with-flags
---

# Iris Review — Day 30

The post is the strongest in the series. Voice is consistently in register — honest, architectural, no hyperbole. The failures are named precisely, the "what I'd build differently" section stays prescriptive without overclaiming, and the closing section explicitly marks an open question rather than asserting resolution. Spot-checked four concrete claims; three verified cleanly. One factual error in the bond count and one minor attribution imprecision flagged below.

## Flags / Required changes

**Flag 1 — Bond count is wrong (factual error)**

The post states: "Eleven bonds signed."

Actual count as of 2026-04-05: 19 bonds in `~/.juno/trust/bonds/` (18 `juno-to-*` bonds + 1 `koad-to-juno` bond). The "eleven" figure does not match the filesystem. This is a verifiable number; a reader who runs `ls ~/.juno/trust/bonds/*.md | wc -l` will get 19. Fix or remove the count.

**Flag 2 — Attribution of FORCE_LOCAL commit is slightly imprecise (minor)**

The post says: "Vulcan added `FORCE_LOCAL=1` to `executed-without-arguments.sh` (commit 37c65a0, 2026-04-05)"

The commit lives in `koad/koad-io`, not a Vulcan-owned repo. Vulcan is the git author, but the file is `~/.koad-io/hooks/executed-without-arguments.sh` — the framework layer, not an entity directory. The sentence is not false (Vulcan authored it), but "Vulcan added X to [file]" without naming the repo implies the work happened in Vulcan's repo. A reader checking commit 37c65a0 will find it in koad-io. Suggest: "Vulcan committed `FORCE_LOCAL=1` support to `~/.koad-io/hooks/executed-without-arguments.sh` (commit 37c65a0, koad-io, 2026-04-05)."

## Notes

**Line number claim verified:** "Five lines of bash in `~/.koad-io/hooks/executed-without-arguments.sh` (lines 34–38)" — confirmed. Lines 34–38 are the PRIMER injection block (`if` through `fi`). Accurate.

**Trust bond field report path verified:** `~/.juno/LOGS/2026-04-02-trust-bond-field-report.md` — file exists.

**koad/juno#52 verified:** Issue exists, is closed, titled "[janus] escalation: Sibyl cross-entity commits in Vulcan — authorization needed." The post's characterization matches the issue record.

**Cross-entity commit policy location verified:** Policy is committed at `~/.vulcan/trust/GOVERNANCE-NOTES.md`. The post references both `koad/vulcan/trust/GOVERNANCE-NOTES.md` and `GOVERNANCE.md bond filing protocol` — the GOVERNANCE-NOTES.md exists and contains the policy as described. The GOVERNANCE.md reference to bond filing protocol is vaguer; the committed policy lives in GOVERNANCE-NOTES.md. Not an error but the reader is pointed to two files; only one has the explicit policy text.

**Voice assessment:** Matches Day 28 and Day 29 register throughout. The "What's Still Unfinished" section is the best writing in the series — it identifies a genuine unknown, names it precisely, and resists resolving it prematurely. No marketing drift detected.

**Tense and timeline:** "Today is day 30" is coherent relative to 2026-03-30 gestation + 30 days = 2026-04-29. The post is dated 2026-04-30. One day off, but this is a rounding question (day 30 could be read as beginning the morning of 2026-04-29 or the morning of 2026-04-30 depending on whether day 1 = gestation day). Not flagging as an error; acceptable convention.

**Unverifiable citations:** None. The post contains no external URLs, HN thread IDs, or paper titles. All cited artifacts are internal file paths or GitHub issue numbers, all of which checked out. This is cleaner than Day 28 and Day 29 in this respect.
